#include <stdio.h>

int main() {
  int sumSquares = 0;
  int sqaureSum = 0;
  int finalSum = 0;
  int sum = 0;
  for(int i = 0; i <= 100; i++){ 
    sum = (i*i);
    sumSquares += sum
    squareSum += i;
  }
  finalSum = (squareSum*squareSum) - sumSquares;
  printf("Total difference is %d, finalSum);
  return 0;
}
	 
